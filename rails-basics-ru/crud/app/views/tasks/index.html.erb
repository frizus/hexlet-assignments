<h1>Tasks</h1>
<ul>
  <li><%= link_to 'Создать', new_task_path %></li>
  <% @tasks.each do |task| %>
    <li><%= link_to task.name, task_path(task) %>
      (статус: <%= task.status %>, <%= task.completed ? 'выполнено' : 'не выполнено' %>)
    </li>
  <% end %>
</ul>